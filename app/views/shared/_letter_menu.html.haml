.letter_menu
  %span.letters.random
    = link_to "random", '#'
  &nbsp; &nbsp;
  %span.letters
    - [*'a'..'z'].each do |letter|
      = link_to letter, define_path({ :char => letter })
  %span.letters
    = link_to '#', define_path( { :char => '#' })
%hr
