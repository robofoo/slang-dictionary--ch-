-per_page = 3

-if !@definitions.nil? && !@definitions.empty?
  = will_paginate @definitions, :class => 'apple_pagination'
  %br
  = render :partial => 'definitions/definitions', :object => @definitions, :locals => { :show_votes => true }
  = will_paginate @definitions, :class => 'apple_pagination'
-else
  .chinese= params[:word]
  not defined
  = link_to "yet", new_definition_path(:word => params[:word])
